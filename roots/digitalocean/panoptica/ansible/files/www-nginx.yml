---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: www-nginx-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: "www.do.iot4.net"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: www-nginx
            port:
              number: 80
---
apiVersion: v1
kind: Service
metadata:
  name: www-nginx
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: www-nginx
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: www-nginx
spec:
  selector:
    matchLabels:
      app: www-nginx
  replicas: 1
  template:
    metadata:
      labels:
        app: www-nginx
    spec:
      containers:
      - name: www
        image: vchokshi/www-nginx
        ports:
        - containerPort: 80
