[build-system]
requires = ["setuptools >= 40.6.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.isort]
extend_skip = [".build"]
skip_glob = ["vendor/*"]

[tool.black]
extend-exclude = '/(\.build|dist|vendor/*|build)/'
