apiVersion: v1
kind: Pod
metadata:
        name: nginx
spec:
        containers:
                - name: nginx
                  image: vchokshi/www-nginx
                  ports: 
                  - containerPort: 80
